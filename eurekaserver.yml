server:
  port: 8070

spring:
  application:
    name: eurekaserver

eureka:
  instance:
    hostname: moodly-eurekaserver

  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://moodly-eurekaserver:8070/eureka